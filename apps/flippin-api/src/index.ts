async function main() {
  console.log("[flippin-api] dev stub running. Replace this with real server startup when ready.");
  // TODO: move your real startup (with await) inside main() later.
}

main().catch((err) => {
  console.error(err);
  process.exit(1);
});
