# Example environment variables for flippin-app (Next.js app)
# Placeholder app — add env vars as features are built out

# Server port (optional)
PORT=3003

# Node environment
NODE_ENV=development
