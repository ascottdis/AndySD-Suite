# Example environment variables for artperiod-app (Next.js app)
# Placeholder app — add env vars as features are built out

# Server port (optional)
PORT=3005

# Node environment
NODE_ENV=development
