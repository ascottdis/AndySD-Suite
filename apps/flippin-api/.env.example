# Flippin API Environment Variables

# Database connection URL (uses app-scoped var to prevent cross-app DB sharing)
FLIPPIN_DATABASE_URL=postgresql://user:password@localhost:5432/flippin_db

# Fallback (for local development only; production should always use FLIPPIN_DATABASE_URL)
DATABASE_URL=postgresql://user:password@localhost:5432/dev_db

# Server port
PORT=3006

# JWT secret (for auth integration with @andysd/auth)
JWT_SECRET=your-secure-random-string-min-32-chars

# Vision API key (for AI attribute extraction — OpenAI, Google Vision, etc.)
VISION_API_KEY=sk-your-api-key

# LLM API key (for title/description generation)
LLM_API_KEY=your-llm-api-key

# Marketplace API keys (optional, for listing/delisting integration)
EBAY_API_KEY=
MERCARI_API_KEY=
FACEBOOK_API_KEY=
