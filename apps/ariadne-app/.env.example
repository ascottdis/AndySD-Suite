# Example environment variables for ariadne-app (Next.js app)
# Placeholder app — add env vars as features are built out

# Server port (optional)
PORT=3006

# Node environment
NODE_ENV=development
