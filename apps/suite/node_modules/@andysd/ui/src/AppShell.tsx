"use client";

import React from "react";

type AppShellProps = {
  children: React.ReactNode;
  title?: string;
};

export function AppShell({ children, title }: AppShellProps) {
  return (
    <div className="min-h-screen bg-gradient-to-br from-andysd-bg via-black to-andysd-bg-soft text-slate-100">
      <div className="mx-auto flex min-h-screen max-w-6xl flex-col px-4 py-6">
        <header className="mb-6 flex items-center justify-between">
          <div className="flex items-center gap-3">
            <div className="h-8 w-8 rounded-lg bg-gradient-to-br from-andysd-primary to-andysd-accent-hot shadow-lg shadow-cyan-500/40" />
            <div className="flex flex-col">
              <span className="text-xs font-semibold tracking-[0.25em] text-slate-400 uppercase">
                ANDYSD SUITE
              </span>
              <span className="text-sm text-slate-300">
                Income OS Â· Mission Control
              </span>
            </div>
          </div>
          {title && (
            <div className="rounded-full border border-slate-800 bg-slate-900/60 px-3 py-1 text-xs text-slate-300">
              {title}
            </div>
          )}
        </header>
        <main className="flex-1">{children}</main>
        <footer className="mt-6 border-t border-slate-800 pt-4 text-xs text-slate-500">
          <div className="flex items-center justify-between">
            <span>Â© {new Date().getFullYear()} ANDYSD Â· Studio</span>
            <span className="text-[0.65rem] text-slate-600">
              Midnight Slate Â· Studio Edition
            </span>
          </div>
        </footer>
      </div>
    </div>
  );
}
