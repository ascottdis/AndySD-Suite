import "./portfolio.css";

export const metadata = {
  title: "AndySD | Cinematic Portfolio",
  description: "AndySD cinematic portfolio home",
};

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
