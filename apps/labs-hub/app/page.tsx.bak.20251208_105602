export default function Page() {
  return (
    <main className="min-h-screen bg-black">
      <iframe
        src="/legacy/index.html"
        title="AndySD Labs — Portfolio"
        className="w-full h-screen border-0"
        loading="eager"
      />
    </main>
  );
}
