# Example environment variables for giggin-app (Next.js app)
# No database required for this app (frontend only)

# Server port (optional, defaults to 3001)
PORT=3001

# API endpoint (if connecting to giggin-api backend)
NEXT_PUBLIC_API_URL=http://localhost:3001

# Node environment
NODE_ENV=development
