# Example environment variables for the root monorepo
# Use for local development only

# Global fallback database URL (used if app-specific vars are not set)
# Each app should prefer its own env var (e.g., GIGGIN_DATABASE_URL)
DATABASE_URL=postgresql://user:password@localhost:5432/andysd_dev

# For drizzle-kit CLI operations
DRIZZLE_DATABASE_URL=${DATABASE_URL}

# JWT secret for auth (shared across auth routes)
JWT_SECRET=dev-secret-change-in-production

# Node environment
NODE_ENV=development
