# Example environment variables for labs-hub (Next.js portfolio site)
# No database required for this app

# Server port (optional, defaults to 3000)
PORT=3000

# Node environment
NODE_ENV=development
